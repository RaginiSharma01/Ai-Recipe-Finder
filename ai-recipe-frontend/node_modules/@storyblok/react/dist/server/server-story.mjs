import { jsx as l } from "react/jsx-runtime";
import { forwardRef as c } from "react";
import i from "./server-component.mjs";
const u = c(
  ({ story: e, ...o }, t) => {
    var r;
    if (!e)
      return console.error(
        "Please provide a 'story' property to the StoryblokServerStory"
      ), null;
    if ((r = globalThis.storyCache) != null && r.has(e.uuid) && (e = globalThis.storyCache.get(e.uuid), globalThis.storyCache.delete(e.uuid)), typeof e.content == "string")
      try {
        e.content = JSON.parse(e.content);
      } catch (n) {
        console.error(
          "An error occurred while trying to parse the story content",
          n
        ), e.content = {};
      }
    return /* @__PURE__ */ l(i, { ref: t, blok: e.content, ...o });
  }
);
export {
  u as default
};
